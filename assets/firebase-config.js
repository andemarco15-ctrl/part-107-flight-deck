/*
 * Firebase web config for accounts and synced progress.
 * These values are public by design (Firebase protects data with security
 * rules, not by hiding this config). Leave as null to run without accounts.
 */
window.P107_FIREBASE = null;
